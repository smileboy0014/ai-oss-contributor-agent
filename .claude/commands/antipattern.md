---
description: 발견된 안티패턴을 훅 또는 스킬 체크리스트로 전환합니다. 메모리가 아닌 구조적 차단으로 재발을 방지합니다.
argument-hint: [대상 설명]
---

# /antipattern

`antipattern` 스킬의 슬래시 진입점입니다. 상세 절차는 `skills/antipattern/SKILL.md` 참조.

**인자**: `$ARGUMENTS` → 스킬로 그대로 전달
