/**
 * GitHub 이슈 수집·정규화·필터링.
 *
 * <p><b>소유</b> — 대상 저장소의 이슈 스냅샷, 수집 커서, 필터 판정 이력.
 * <p><b>소유하지 않음</b> — 기여 가능성 판정({@code candidate}) · LLM 호출({@code agent}).
 *
 * <p>아직 비어 있다. 경계를 먼저 그어 두어 {@code candidate} 안에 수집 로직이
 * 섞여 들어가는 것을 구조로 막는다.
 */
package com.ossagent.issue;
